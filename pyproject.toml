[project]
name = "blackwell-inference"
version = "0.1.0"
description = "SM120 Blackwell inference backend repro and benchmark harness"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
